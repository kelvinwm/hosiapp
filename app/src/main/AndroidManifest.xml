<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="knh.or.ke.hospital">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyBookings"
            android:label="@string/title_activity_my_bookings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Clinicavailabity"
            android:label="@string/title_activity_clinicavailabity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Signin" />
        <activity
            android:name=".Clinicslist"
            android:label="@string/title_activity_clinicslist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".content.BookClinic"
            android:label="@string/title_activity_book_clinic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SampleData" />
        <activity
            android:name=".Reschedule"
            android:label="@string/title_activity_reschedule"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".content.Direction"
            android:label="@string/title_activity_direction"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".content.HealthTips"
            android:label="@string/title_activity_health_tips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".content.Campigns"
            android:label="@string/title_activity_campigns"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>